class Solution:
    def prefixesDivBy5(self, nums):
        answer = []
        current = 0
        
        for num in nums:
            # Update current value: left shift and add current bit
            current = ((current * 2) + num) % 5
            # Check if divisible by 5
            answer.append(current == 0)
        
        return answer
